[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"

# Allow the AI analysis route sufficient time for web research + model calls
[functions]
  timeout = 90
